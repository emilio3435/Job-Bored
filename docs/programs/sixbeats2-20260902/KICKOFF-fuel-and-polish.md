# Lane fuel-and-polish — NEW-3, NEW-12, NEW-9, NEW-5 (BLOCKER), NEW-13
Read `SIXBEATS2-SPEC.md` (locked decision 5), the ground rules, spec §5 B5. Fence: `oneflow-beat-discovery.js`, `dev-server.mjs` (one new route + tests), `discovery-wizard-verify.js`, `oneflow-demo-board.js`, the single toast CSS rule (find where `.toast` is styled; one rule: max-width + overflow-wrap), `css/oneflow.css` L3/L4 regions.
Mission, red-first per item: (NEW-3) `/__proxy/serpapi-check` calls SerpApi's account endpoint server-side with the same local-origin auth as sibling proxies; the beat reports connected only on `ok` and shows the real quota; invalid/unreachable → message slot with a next action; dev-server test with a stub upstream. (NEW-12) the ts.net "Can't reach the endpoint." message names the first check to run. (NEW-9) numbered steps render as a real ordered list (marker on its own line start). (NEW-5) the demo-card detail has a Close control and Escape closes it; the pill stays clickable while it is open (bounding-box test). (NEW-13) a long toast wraps at ≤ 420 px, never one word per line. Full floor + `test:e2e-visual`. Commit locally, never push.
