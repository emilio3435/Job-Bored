# Lane drafting-provider — NEW-2 (BLOCKER), NEW-8, NEW-11, and Beat 3's side of NEW-7
Read `SIXBEATS2-SPEC.md` (locked decisions 3 and 4), the ground rules, spec §5 B2–B3. Fence: `oneflow-beat-resume.js`, `oneflow-beat-ai.js` (provider defaults/labels only), `server/profile-from-resume.mjs` (+ its tests).
Mission: Beat 3 drafts through whatever Beat 2 verified. Red-first: (1) server test — a request body carrying `{provider:"openrouter", apiKey, model}` drafts via the OpenAI-compatible path and never reads Gemini env; same for openai/local/anthropic; an error names the provider, never an env var; (2) beat test — Beat 3 sends the verified config from `getResumeGenerationConfig()`; (3) Beat 2: OpenRouter pre-selected + "Recommended", Gemini pin `gemini-3.5-flash`; (4) Beat 3 calls `ctx.saveDraft("resumeText", text)` on input (debounce is the controller's job) and `ctx.saveDraft("profileDraft", draft)` on draft ready, and restores both from `ctx.runtime.drafts` on render — stub `saveDraft`/`runtime.drafts` to the contract shape in your tests. Real-browser proof: fresh greenfield → OpenRouter key → paste resume → `Draft from this text` → 200 and a drafted Beat 4, network lines pasted. Full floor. Commit locally, never push.
