# Lane finale — NEW-1 (BLOCKER), NEW-10, NEW-4
Read `SIXBEATS2-SPEC.md` (locked decision 2), the ground rules, spec §5 B6. Fence: `onboarding-celebration.js`, `oneflow-beat-payoff.js`, `oneflow-beat-ai.js` (auto-advance delay only), `css/oneflow.css` CORE.
Mission: make the finale a non-blocking burst over a clickable Beat 6. Red-first: (1) a Playwright real-browser probe that reaches Beat 6 and clicks `Run discovery now` WITHOUT dismissing anything programmatically — fails today (overlay intercepts); (2) unit probes: the finale overlay has `pointer-events: none`, no journey strip, no alt link, no CTA, auto-fades ≤ 3 s, reduced-motion path still works; (3) Beat 6 sub-line appears exactly once; (4) Beat 2 holds the success line ≥ 1.2 s before advancing. Update the celebration tests that pinned the CTA-gated modal. Full floor + Playwright proof pasted. Commit locally, never push.
