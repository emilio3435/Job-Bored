# Lane draft-persistence — NEW-14 (BLOCKER), NEW-6, and the controller side of NEW-7
Read `SIXBEATS2-SPEC.md` (locked decisions 4 and 6), the ground rules, spec §3.2/§3.4. Fence: `onboarding-flow.js`, `user-content-store.js` (normalize `drafts` only), `oneflow-beat-fit.js`, `css/oneflow.css` CORE (pill rule only). Land the controller API in your FIRST commit — drafting-provider codes against it.
Mission: drafts survive refresh; a paused flow can always be resumed. Red-first: (1) controller — `ctx.saveDraft(key, value)` persists into flow state (debounced), `ctx.runtime.drafts` is hydrated on `open()`/`goToBeat()`, unknown keys rejected, `clearOnboardingFlowState` wipes drafts; (2) Beat 4 renders from `runtime.drafts.profileDraft` when present and completes after a simulated reload (the NEW-14 repro: open fit → reload harness → open fit → `Looks like me →` succeeds); (3) close hook: when `#oneFlowDemoBoard` is not active, closing renders a "Resume setup ▸" pill that reopens the saved beat; hidden after flow completion; a11y label. Real-browser proof for (2): Playwright greenfield → template draft → Beat 4 → page.reload() → Beat 4 populated → advance succeeds. Full floor. Commit locally, never push.
