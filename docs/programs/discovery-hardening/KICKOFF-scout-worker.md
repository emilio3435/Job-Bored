# Scout lane: discovery-hardening-scout-worker (READ-ONLY)

Goal: Give the orchestrator exact, file-and-line evidence for claims LIFECYCLE-1, CANARY-1, and the worker/secret half of STABLE-1 so implementation lanes can start from executable RED probes.

Success means: `docs/programs/discovery-hardening/SCOUT-worker.md` exists in this worktree and, for each claim below, holds (a) exact file paths + line refs, (b) current behavior, (c) missing assertions, (d) smallest credible change and which existing pattern it reuses, (e) likely ownership conflicts with other lanes, (f) an executable RED probe: the exact command to run and the exact failing output you observed when you ran it (paste it). Create no product changes. Create no commits. You MAY write throwaway probe tests under `.lane-evidence/` and run them.

Stop when: the scout file is complete for all three claims, or you hit a blocker you paste verbatim.

Read first: `docs/programs/discovery-hardening/GROUND-RULES.md`, `docs/programs/discovery-hardening/PROGRAM-SPEC.md` (draft), `AGENTS.md`, `AGENT_CONTRACT.md` if present.

## LIFECYCLE-1 — accepted, running, retryable polling, terminal success/failure, duplicate delivery, exactly-once Sheet effects

Inspect `integrations/browser-use-discovery/src/webhook/handle-discovery-webhook.ts` (1402 lines; order invariant: method check → secret auth → JSON parse → per-run `googleAccessToken` strip → preflight → first run-status side effect → run), `src/state/run-status-store.ts` (`buildAccepted/Running/Completed/FailedRunStatus`, `createDiscoveryRunStatusStore`), `src/sheets/discovery-runs-writer.ts` (`createTerminalHistoryFinalizer`, `appendDiscoveryRunRow`), `src/sheets/pipeline-writer.ts`, `src/server.ts` (`/runs/{runId}` ~1078, `/health` ~1073), `src/contracts.ts` (statusPath ~542, ~1041), and the browser side `discovery-status-handoff.js` (`buildRunStatusUrl` ~514, `statusPath || status_path` ~542, polling ~600-700, ~900-1100), `discovery-run-orchestration.js` ~392.
Existing tests: `tests/webhook/handle-discovery-webhook.test.ts` (4269 lines — list which lifecycle states it already covers by test name), `tests/webhook/run-discovery-runs-log.test.ts`, `tests/state/run-status-store.test.ts`, `tests/sheets/discovery-runs-writer.test.ts`, `tests/sheets/pipeline-writer.test.ts`, root `tests/discovery-*.test.mjs` for the polling side.
Questions: Build a matrix: state/behavior (accepted, running, retryable poll [which HTTP statuses/shapes are retryable vs terminal on the browser side], terminal completed, terminal failed, duplicate webhook delivery for the same runId, exactly-once DiscoveryRuns row, exactly-once Pipeline write) × proving test (file:line) or NONE. Is there ANY duplicate-delivery / idempotency guard today (grep runId reuse, `alreadyRunning`, status store `has`)? If none: what is the smallest credible guard (orchestrator's working assumption: the run-status store already holds the runId → return the existing accepted status instead of starting a second run; confirm whether the runId is client-supplied or server-generated, because that decides whether duplicate delivery is even distinguishable). Does the browser poller accept both `statusPath` and `status_path` — cite the line and the test. Write RED probes for every NONE row; paste failing output.

## STABLE-1 (worker/secret half)

Inspect the secret flow: how `DISCOVERY_WEBHOOK_SECRET` (or the repo's actual name — find it in `src/config.ts` and `.env.example`) reaches the worker, `hasValidWebhookSecret` in `handle-discovery-webhook.ts` ~1337, `run-status-auth.ts`, `scripts/lib/discovery-transport.mjs` (201 lines), `scripts/bootstrap-local-discovery.mjs` (Tailscale/stable transport paths, `/health` probes ~956-1090, ~1224, ~1476-1640), `dev-server.mjs` Tailscale handling, and the six root tests named in the spec (run each with `npm test -- <file>`, paste results). Also `docs/` for the stable-transport / Tailscale runbook.
Questions: Is the stable transport + secret handoff already proven end to end by existing tests (cite each)? Is there ONE evidence-backed gap (e.g. the worker returns 401 vs 403 and the UI cannot tell "wrong secret" from "no secret configured", or the readiness snapshot cannot distinguish "tunnel down" from "worker down")? State plainly: already sufficient, or one gap with a RED probe.

## CANARY-1 — one read-only command classifies worker health and recent discovery success

Inspect reusable readers: `scripts/discovery-keep-alive.mjs` (`isExpectedWorkerHealthPayload` ~176, `verifyNgrokWorkerIdentity` ~186), `scripts/bootstrap-local-discovery.mjs` health probe helpers, `scripts/doctor.mjs` (`check()`, `summarize()`, `runDoctor`, `formatDoctorReport`, `parseArgs` — the closest existing CLI pattern for a status command), `scripts/discovery-shared-helpers.mjs` ~108, `integrations/browser-use-discovery/src/server.ts` `/health` payload (mock at `tests/mocks/health-response.ok.v1.json`) and `/runs/{runId}`, `src/sheets/discovery-runs-writer.ts` (`parseDiscoveryRunsCells` ~102 — the DiscoveryRuns row shape), `src/state/run-status-store.ts` (on-disk run-status directory under `integrations/browser-use-discovery/state/` — is there a local run history the canary can read WITHOUT Sheets credentials?), and root `runs-tab.js` / `discovery-run-tracker.js` for how the dashboard reads DiscoveryRuns.
Questions: Which reader should the canary reuse for (1) worker health and (2) newest successful run — prefer a local, credential-free source if one exists; say whether Sheets is needed and how the token would be obtained read-only (and whether that is acceptable or should be `unavailable`). Which existing script has the cleanest `parseArgs` + injected `fetchImpl`/`now` pattern to copy? Propose the status → exit-code mapping consistent with `doctor.mjs` conventions. Where is `package.json` `scripts` ordering — the canary must add exactly one script. Which tests directory + glob will pick up `tests/discovery-canary.test.mjs`? Write a RED probe: `npm run discovery:canary -- --max-age-hours 24 --json` and paste the failure.

## Output format

Write `docs/programs/discovery-hardening/SCOUT-worker.md` with one `## <CLAIM-ID>` section per claim, each with the six sub-headings (a)–(f). End with `## Ownership conflicts` listing any file two lanes would both need, and `## Environment` listing anything you could not run (with the verbatim error). Do not commit.
