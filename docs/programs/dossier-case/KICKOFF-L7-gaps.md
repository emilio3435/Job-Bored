# Kickoff · L7 gaps (claim H) — close the four capability gaps L5 found in The Case

Read `docs/programs/dossier-case/GROUND-RULES.md` first. Your branch is cut from `feat/dossier-case` after the L5 cutover: The Case is live in `role.js`, the Brief is deleted, and the full floor plus `npm run smoke:jb-v2` are green. Read `docs/programs/dossier-case/reports/L5-report.md` §5 — it names each gap with file and line.

**Mission.** The Case must carry every user-facing capability the Brief had that the spec did not deliberately cut. Four items, each test-first, each its own commit:

1. **Location and salary are editable in the rail** (spec §5 table). In `role-case.js` `renderRail`, render location and salary as inline `edit-field` inputs (`data-field="location"` / `"salary"`, `data-original`, `aria-label`, the same `GUARDS`), styled as inline fact inputs on the navy rail (`.case__fact-input`: `font: inherit; color: inherit; background: transparent; border: none; border-bottom: 1px dashed transparent; field-sizing: content; width: 12ch; max-width: 100%`, hover/focus underline as the title). Restore the `ch`-width fallback in `role.js` `wireDossier` for `.case__fact-input` where `field-sizing` is unsupported (the code L5 removed is in `git show 628a69a:role.js`). Tests: extend `tests/role-case-render.test.mjs` (inputs present with values, escaping) and `tests/role-field-edit-render-guard.test.mjs` (blur commits `location`/`salary`, guard defers while focused).
2. **Recruiter strip mounts in The Case.** The Brief mounted `<div data-mount="recruiter-strip">` and called `JobBoredRecruiterStrip.render(...)` (see `git show 628a69a:role-brief.js`, lines ~804–815, for the exact call and its arguments). Render the mount at the bottom of the YOUR MOVES lane, under People, and make the same call from `role.js` after `Case.render(...)` (it is a `role.js` concern, not the renderer's). Tests: `tests/role-case-interactions.test.mjs` asserts the mount exists after render and that `JobBoredRecruiterStrip.render` was invoked with the open job; keep `tests/recruiter-strip-dossier.test.mjs` green.
3. **Skeleton announces to screen readers.** `role-case.js` `skeletonRows()` gains `role="status" aria-live="polite"` alongside `aria-busy="true"`, and the visible status line the Brief had ("Reading the posting…") returns as one `<span class="case__skeleton-status">` inside the THEY WANT lane's skeleton. Tests: `tests/role-case-render.test.mjs` pins all three attributes and the status line while `loading.enrichment` is true.
4. **Provenance surfaces in The Case** (orchestrator decision, 2026-09-02): the classifier and validator are live but nothing renders them. Add to the model (`role-case-model.js`): `provenance: { parseMode: enrichment.parseMode, reviewState: validator reviewState if present, freshness: label from dossier-field-provenance's freshness helper, inferredFields: [field names the classifier marks "inferred"] }` — read exactly what `dossier-field-provenance.js` and `structured-output-validator.js` expose (`grep -n "root\.\|window\." dossier-field-provenance.js structured-output-validator.js`) and consume those APIs; do not re-derive. Render: when `parseMode` is a recovered/repaired mode or `reviewState.status === "needs_review"`, the THEY WANT lane head shows a `case__src case__src--review` tag reading `recovered parse · review` and the lane's `.case__sub` for requirements reads "Requirements · recovered parse — review before relying on these"; when title/company grounding is `inferred`, the rail's meta gains an `inferred` source tag; the freshness label renders as `.case__stamp` under the one-line quote when present. Tests: restore the three retired rendered-label cases from `git show 628a69a:tests/dossier-provenance-labels.test.mjs` and `git show 628a69a:tests/dossier-field-provenance.test.mjs`, retargeted to The Case's markup — same intent, same fixtures, new selectors; never assert only absence.

**Fence.** `role-case.js`, `role-case-model.js`, `role-case.css`, `role.js` (rail fallback + recruiter-strip call only), `tests/role-case-render.test.mjs`, `tests/role-case-interactions.test.mjs`, `tests/role-field-edit-render-guard.test.mjs`, `tests/dossier-provenance-labels.test.mjs`, `tests/dossier-field-provenance.test.mjs`. Nothing else.

**Non-negotiables.** Every new CSS rule scoped under `body.jb-v2 [data-region="role"] .case`; raw hex only inside custom-property definitions (`node tools/lint-tokens.mjs` → 0 findings). No vendor names. Escape exactly once. `npm run smoke:jb-v2` stays 13/13.

**Definition of Done.**
1. `node --test tests/role-case-render.test.mjs tests/role-case-interactions.test.mjs tests/role-field-edit-render-guard.test.mjs tests/dossier-provenance-labels.test.mjs tests/dossier-field-provenance.test.mjs tests/recruiter-strip-dossier.test.mjs tests/role-case-model.test.mjs` green.
2. Full floor green (ground rules §Floor) + `npm run smoke:jb-v2` + `node tools/lint-tokens.mjs` — pasted in `LANE-REPORT-L7.md` §4.
3. Four commits: `feat(dossier): editable location and salary in the Case rail`, `feat(dossier): mount the recruiter strip in the Case`, `fix(dossier): announce the Case loading skeleton to screen readers`, `feat(dossier): surface parse provenance and freshness in the Case`.
