# 3E mock package (Materials v2)

Acceptance snapshot for **AI & Marketing Analytics Manager @ 3E** (Bethesda / Eastern remote). Prose is rewritten under v2 rules from Emilio’s verified themes (Elio, Audacy attribution, SEM forecast). It is not a clone of the Materials Queue dump.

| File | Use |
|---|---|
| `resume.html` | Print-ready 1-page resume (US Letter) |
| `cover-letter.html` | Print-ready 1-page letter (~220 words) |
| `resume.txt` / `cover-letter.txt` | ATS twins |
| `jd-extract.json` | Stage 1 fixture |
| `claim-selection.json` | Stage 2 fixture |

Open the HTML in a browser → Print preview → US Letter. One `.page` each. Do not shrink type.

QA notes this package would still raise:

- `constraint_conflict` (review) — contact is Denver; 3E’s CO authorization excludes Denver.
- `transfer_overclaim` must stay **absent** — Power BI is not listed as a skill.
