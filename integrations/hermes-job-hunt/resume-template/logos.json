{
  "$comment": "NEUTRAL EXAMPLE logo manifest for the sample resume template. Copy this pattern into your own template root (never commit real employers here). Resolved by scripts/logo_resolver.py into assets/logo-<slug>.png. Priority per entry: upload (a file in uploads/) > favicon (fetched from domain) > monogram (generated initials, never fails). The <slug> must match the resume.html <img src=\"assets/logo-<slug>.png\"> marks.",
  "logos": [
    {
      "slug": "northwind",
      "label": "Northwind",
      "domain": "northwind.example.com"
    },
    {
      "slug": "sample-co",
      "label": "Sample Co",
      "upload": "uploads/logo-sample-co.png"
    }
  ]
}
